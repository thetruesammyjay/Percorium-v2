# Data workspace

`raw/` stores auditable provider payloads. `processed/` stores normalized datasets used by the API and research jobs. Do not commit secrets, wallet data, or unlicensed bulk market data.
